% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/appraise_gdam.R
\name{appraise.gdam}
\alias{appraise.gdam}
\title{Model diagnostics plots for gdam objects}
\usage{
\method{appraise}{gdam}(
  model,
  use_worm = FALSE,
  n_uniform = 10,
  n_simulate = 50,
  method = "uniform",
  type = "deviance",
  n_bins = "sturges",
  ncol = NULL,
  nrow = NULL,
  guides = "keep",
  level = 0.9,
  ci_col = "black",
  ci_alpha = 0.2,
  line_col = "red",
  ...
)
}
\arguments{
\item{model}{A fitted \code{gdam} object.}

\item{use_worm}{Logical. Should the worm plot be drawn in place of the QQ plot? Default is \code{FALSE}.}

\item{n_uniform}{Integer. The number of times to randomize uniform quantiles in the direct computation method or QQ plots.}

\item{n_simulate}{Integer. The number of times to simulate from the estimated model in the simulation-based method for QQ plots.}

\item{method}{Character. The method to use for to generate theoretical quantiles. Options are "uniform" or "simulate".}

\item{type}{Character. The type of residuals to use. Options are "deviance" or "pearson" residuals.}

\item{n_bins}{Character. The number of bins to use in the histogram of residuals. See \code{\link[gratia:appraise]{gratia::appraise()}} for more details.}

\item{ncol}{Integer. The number of columns in the grid of plots. If \code{NULL}, the number of columns is determined automatically.}

\item{nrow}{Integer. The number of rows in the grid of plots. If \code{NULL}, the number of rows is determined automatically.}

\item{guides}{Character. The guide to use for the color scale. See \code{\link[gratia:appraise]{gratia::appraise()}} for more details}

\item{level}{Numeric. The confidence level for the reference intervals in the QQ plot. Only used if \code{method = "simulate"}.}

\item{ci_col}{Character. The color of the reference interval lines in the QQ plot. Only used if \code{method = "simulate"}.}

\item{ci_alpha}{Numeric. The alpha transparency level of the reference interval lines in the QQ plot. Only used if \code{method = "simulate"}.}

\item{line_col}{Character. The color of the reference 1:1 line in the QQ plot, and the reference line in the residuals vs. linear predictor plot.}

\item{...}{Currently not used.}
}
\value{
A set of model diagnostic plots as per the original \code{\link[gratia:appraise]{gratia::appraise()}} function.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

This function is basically a wrapper around the \code{\link[gratia:appraise]{gratia::appraise()}} function, but designed to work with \code{gdam} objects. The main difference is that observations (residuals, quantiles etc...) in the diagnostic plots are colored by the final weights from the \code{gdam} object.
}
\note{
Acknowledgments to Gavin Simpson and the \code{gratia} package for the original \code{\link[gratia:appraise]{gratia::appraise()}} function.
}
\examples{
\donttest{
# See the main `gdam` help file for examples.
}

}
\author{
Francis K.C. Hui \href{mailto:fhui28@gmail.com}{fhui28@gmail.com}
}
