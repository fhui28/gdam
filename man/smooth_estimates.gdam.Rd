% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_estimates_gdam.R
\name{smooth_estimates.gdam}
\alias{smooth_estimates.gdam}
\title{Evaluate smooths from a fitted gdam model at covariate values}
\usage{
\method{smooth_estimates}{gdam}(
  object,
  select = NULL,
  n = 100,
  n_3d = 16,
  n_4d = 4,
  data = NULL,
  dist = NULL,
  partial_match = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted \code{gdam} object.}

\item{select}{A character vector of smooths to evaluate. If \code{NULL}, all smooths are evaluated.}

\item{n}{The number of points at which to evaluate the smooths.}

\item{n_3d}{The number of points over the range of the last covariate must in a 3D plot; see \code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}} for details.}

\item{n_4d}{The number of points over the range of the last covariate in a 4D plot; see \code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}} for details.}

\item{data}{A data frame containing the values of the covariates at which to evaluate the smooths. If \code{NULL}, the original data is used.}

\item{dist}{Numeric; if greater than zero, this is used to determine when a location is too far from data to be plotted when plotting 2D smooths; see \code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}} for details.}

\item{partial_match}{Logical. In the case of character \code{select}, should \code{select} match partially against \code{smooths}? If \code{TRUE} then \code{select} only be a single string.}

\item{...}{Currently not used.}
}
\value{
A data frame which is of class \code{smooth_estimates}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

This function is basically a wrapper around the \code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}} function for valuating a smooth at a grid of evenly spaced value over the range of the covariate, but designed to work with \code{gdam} objects. Please see the associated help file in the \code{gratia} package for more details.
}
\note{
Acknowledgments to Gavin Simpson and the \code{gratia} package for the original \code{\link[gratia:smooth_estimates]{gratia::smooth_estimates()}} function.
}
\examples{
\dontrun{
See the main `gdam` help file for examples, particularly for constructing partial smooth plots.
}

}
\author{
Francis K.C. Hui \href{mailto:fhui28@gmail.com}{fhui28@gmail.com}
}
